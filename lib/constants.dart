import 'package:flutter/material.dart';

const primaryColorWeek = Color(0xFFA0AA60);
const primaryColorStrong = Color(0xFF58692E);
const secondColor = Color(0xFFFDD282);

const double defaultPadding = 16.0;
